spring:
  application:
    name: project-99
  output:
    ansi:
      enabled: always
  jpa:
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
rsa:
  private-key: classpath:certs/private.pem
  public-key: classpath:certs/public.pem

